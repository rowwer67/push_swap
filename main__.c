#include "push_swap.h"

int main(int argc, char **argv)
{
    int a;
	t_stack_data *basis;
    t_bench *op_count;


    if (argc == 0)
        return (0);
    if (parse_it(argc, argv, basis))
        {
            //printing error message and abort programm
        };
}
