#include "push_swap.h"

int main(int argc, char **argv)
{
    t_stack_data *basis;
    if (argc == 0)
        return (0);
    if (parse_it(argc, argv, basis))
        {
            //printing error message and abort programm
        };
}
